# Base URL of the main backend (Member 1's server, POST /init + GET /feed).
VITE_API_URL=http://localhost:3001

# Base URL of the agent control server (agent/control-server.js),
# used by the "Create Agent" form and Run Now/Pause/Resume controls.
VITE_CONTROL_URL=http://localhost:4000
