// The active agent's id is now created dynamically (see CreateAgentForm
// + App.jsx) and stored in localStorage for the browser session, rather
// than hardcoded here. This key is where it's stored.
export const AGENT_ID_STORAGE_KEY = "activeAgentId";
